import XCTest
@testable import ReusableCollectionView

final class ReusableCollectionViewTests: XCTestCase {
    func testExample() throws {
        // This is an example of a functional test case.
        // Use XCTAssert and related functions to verify your tests produce the correct
        // results.
//        XCTAssertEqual(ReusableCollectionView().text, "Hello, World!")
    }
}
